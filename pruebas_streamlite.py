# crear el entorno virtual  python -m venv .venv
# se crea la carpeta .venv y dentro de esta carpeta esta la carpeta bin donde esta el activador del ambiente virtual
# activamos el hambiente virtual:  source .venv/bin/activate
# instalamos la libreria con :  pip install streamlit
# desactivamos el ambiente  : deactivate